process {
    withName: SENTIEON_CLI {
        queue = { task.attempt > 2 ? 'ebc-ondemand-cpu' : 'ebc-spot-cpu' }
    }
    withName: MULTIQC {
        queue = { task.attempt > 2 ? 'ebc-ondemand-cpu' : 'ebc-spot-cpu' }
    }
}